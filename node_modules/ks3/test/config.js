require('dotenv').config()
const { env } = process

const config = {
    ak: env.AK,
    sk: env.SK,
    bucketName: env.bucketName || 'ks3-sdk-test',
    region: env.region || 'BEIJING',
    endpoint: 'ks3-cn-beijing.ksyuncs.com'
}

module.exports = config