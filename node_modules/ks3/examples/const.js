require('dotenv').config()
var ak = process.env.AK || '<your ak>'; // input your ak
var sk = process.env.SK || '<your sk>'; // input your sk

var bucketName = process.env.bucketName || 'test-bucket' || '<BUCKE_TNAME>'; // input your bucketname

var endpoint = process.env.endpoint || 'ks3-cn-beijing.ksyuncs.com'; // replace your region

var krn = process.env.KRN || '' // replace your rolekrn
module.exports = {
    ak, 
    sk,
    bucketName,
    endpoint,
    krn
}